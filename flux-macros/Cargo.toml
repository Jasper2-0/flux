[package]
name = "flux-macros"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Procedural macros for the Flux operator graph system"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
